<h1>New Pin</h1>

<div class="form">
  <%= form_for [@board,@pin] do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :image_url %>
  <%= f.text_field :image_url %>

  <%= f.submit "Pin That Shit!" %>
  <% end %>
</div>
